{
  "name": "@cloudflare/artifact-fs-sandbox-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Cloudflare Sandbox SDK example that mounts a Git repo with ArtifactFS.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "typecheck": "npm run types:check && tsc --noEmit",
    "types": "wrangler types --include-runtime=false --env-interface=WorkerBindings",
    "types:check": "npm run types -- --check"
  },
  "devDependencies": {
    "@cloudflare/sandbox": "0.12.4",
    "@cloudflare/workers-types": "5.20260727.1",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3",
    "wrangler": "4.114.0"
  }
}
